
import React from 'react';
import ReactDOM from 'react-dom';

/**
 * El primer parametro es el elemento que queremos renderizar
 * El segundo es donde
 */
ReactDOM.render(
    <h1>Hello, world!</h1>,
    document.getElementById('root')
);